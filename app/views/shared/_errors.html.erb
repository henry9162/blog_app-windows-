<section>
  <% if obj.errors.any? %>
    <div class="row mb-3">
      <div class="col-offset-2 col-8">
        <div class="card container border-0 bg-danger">
          <div class="card-heading">
            <h2 class="card-title"><%= pluralize(obj.errors.count, "error") %> prohibited this article from saving </h2>
          </div>
          <div class="card-body">
            <ul>
              <% obj.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <%end%>
            </ul>
          </div>
      </div>
    </div>
  <%end%>
</section>